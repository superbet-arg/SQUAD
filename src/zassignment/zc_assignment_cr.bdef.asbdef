projection implementation in class ZBP_C_ASSIGNMENT_CR unique;
strict ( 2 );
use draft;
define behavior for ZC_ASSIGNMENT_CR alias ZcAssignmentCr
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}